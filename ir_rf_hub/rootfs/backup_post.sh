#!/command/with-contenv bashio
# shellcheck shell=bash
# Nothing to do after backup today; kept as an explicit hook point since
# config.yaml declares backup_post and future migrations may need it.
exit 0
